{
  "name": "js-test",
  "version": "1.0.0",
  "description": "Node.js app for MangoHoster",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npm install --production --no-audit --no-fund"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "cacheDirectories": ["node_modules"]
}
